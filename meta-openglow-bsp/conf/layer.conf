LAYERSERIES_COMPAT_openglow = "thud"

BBPATH .= ":${LAYERDIR}"
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "openglow"
BBFILE_PATTERN_openglow := "^${LAYERDIR}/"
BBFILE_PRIORITY_openglow = "8"

LICENSE_PATH += "${LAYERDIR}"

# Required for Xenomai
HOSTTOOLS += " autoconf automake autoreconf aclocal libtoolize m4 "
